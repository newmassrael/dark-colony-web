version: '3'
services:
  relay:
    build:
      context: .
      dockerfile: Dockerfile.relay
    ports:
      - "8766:8766"
    restart: unless-stopped

  # WSS (internet-facing) — uncomment and configure
  # relay-wss:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.relay
  #   ports:
  #     - "443:443"
  #   volumes:
  #     - /etc/letsencrypt:/certs:ro
  #   command: >
  #     python /app/relay_server.py --port 443
  #       --cert /certs/live/relay.example.com/fullchain.pem
  #       --key  /certs/live/relay.example.com/privkey.pem
  #   restart: unless-stopped
